# Pinned to OLD versions on purpose so SCA tools (pip-audit, Dependabot)
# raise alerts. DO NOT use these versions in real projects.
Flask==3.1.3
Jinja2==3.0.3
Werkzeug==2.0.3
itsdangerous==2.0.1
MarkupSafe==2.0.1
click==8.0.4